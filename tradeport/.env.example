# Frontend — API base URL (no secrets here)
# Local dev with worker: leave empty (Vite proxies /api → localhost:8787)
# Production: your Worker URL, e.g. https://tradeport-api.YOUR_SUBDOMAIN.workers.dev
# VITE_API_BASE_URL=
