# Dev-only overrides (Next.js loads this in `npm run dev`)
NEXT_PUBLIC_BASE_PATH=
NEXT_PUBLIC_WALLET_DNA_API_BASE=
# Faster local analysis — production still uses 5000
WALLET_DNA_MAX_TRANSFERS_PER_CHAIN=1000
