{
  "name": "collection-overlap-api",
  "private": true,
  "description": "Cloudflare Worker — Collection Overlap API only (not FlexGrid)",
  "scripts": {
    "dev": "wrangler dev --local",
    "dev:remote": "wrangler dev --remote",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.54.0"
  }
}
