# Used when you run `npm run build` for GitHub Pages (project site URL).
# Replace `your-repo` with your real GitHub repo name (as in github.com/you/your-repo).
# Must start and end with / — e.g. VITE_BASE_PATH=/ddg/

VITE_BASE_PATH=/your-repo/
