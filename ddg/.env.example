# Optional: GitHub Pages subpath — only if you build locally for deploy.
# Example: VITE_BASE_PATH=/ddg/
# CI auto-sets base from the repo name; you usually do not need this locally for `npm run dev`.

VITE_BASE_PATH=
