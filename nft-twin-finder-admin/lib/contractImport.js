/**
 * Phase 2 — contract-address import (not implemented in V1).
 * Future: chain + contract → auto-fetch metadata via worker/API.
 * @module
 */

/** @returns {null} */
export function importFromContract() {
  return null;
}
